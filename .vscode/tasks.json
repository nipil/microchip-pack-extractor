{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Generate Flamegraph",
            "type": "shell",
            "command": "inferno-flamegraph tracing.folded > flamegraph.svg && inferno-flamegraph --flamechart tracing.folded > flamechart.svg",
            "problemMatcher": [],
            "group": {
                "kind": "build",
            }
        }
    ]
}